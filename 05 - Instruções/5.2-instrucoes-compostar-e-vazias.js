{
  x = Math.PI;
  cx = Math.cos(x);
  console.log("cos(π) = " + cx);
}

//--------------------------//

for(i = 0; i < a.length; a[i++] = 0) ;

//---------------------------//

if ((a == 0)  || (b == 0));         // Essa linha não tem ação
  o = null;                         // e esta é executada


  //----------------------//

  for(i = 0; i < a.length; a[i++] = 0) /* vazio */ ;
